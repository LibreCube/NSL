noobpro_scriptStatus = (getRunScriptSetting "noobpro");

// End if init settings

// Is the script active?
if (= (strcmp $noobpro_scriptStatus "false") 0) [
	noobpro = [
		noobpro_param = $alg1;
		
		if (= $noobpro_param "reset") [
			disconnect
			texreduce 0
			fullbrightmodels 60
			waterrefract 1
			sleep 1000 [
				reconnect
			]
		][
			disconnect
			texreduce 12
			fullbrightmodels 200
			waterrefract 0
			sleep 1000 [
				reconnect
			]
		]
	]
]